import 'package:flutter/material.dart';

class AddTicketianView extends StatelessWidget {
  const AddTicketianView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}